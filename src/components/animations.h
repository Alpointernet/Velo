#ifndef ANIMATIONS_H
#define ANIMATIONS_H

#include "../globals.h"

void TriggerZenTopAnimation(HWND hwnd, bool show);
void TriggerZenBottomAnimation(HWND hwnd, bool show);
void UpdateZenAnimations(HWND hwnd);
void UpdateTabAnimations(HWND hwnd);

#endif // ANIMATIONS_H
